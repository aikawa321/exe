<div class="main clearfix">
    <div class="main-content">
        <div class="body">
                <div class="container">
                    <div class="row">
                        <div class="col-xs-6">
                            <h1>プロフィール編集</h1>
                        <%= form_for(@user) do |f| %>
                        <%= render'users/profile_form', f: f %>
                         <%= f.submit "変更を保存", class:"btn" %>
                        <% end %>
                        </div>
                    </div>
                </div>
            </div>
        <p><%= link_to "退会についてはコチラ", '#' %></p>
    </div>
  </div>
